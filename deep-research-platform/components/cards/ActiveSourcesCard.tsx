import { BookOpen, FileText, Globe, Newspaper } from "lucide-react";
import { GlassCard } from "@/components/ui/GlassCard";

export function ActiveSourcesCard({ counts }:{counts:{total:number;journal:number;preprint:number;web:number}}){const stats=[{label:"Total",value:counts.total,icon:BookOpen},{label:"Journals",value:counts.journal,icon:FileText},{label:"Preprints",value:counts.preprint,icon:Newspaper},{label:"Web",value:counts.web,icon:Globe}]; return <GlassCard className="p-4"><h3 className="mb-3 text-sm font-semibold">Active Sources</h3><div className="grid grid-cols-2 gap-2">{stats.map(s=><div key={s.label} className="rounded-lg border border-ds-border/70 bg-black/20 p-2"><s.icon size={13} className="mb-1 text-ds-muted"/><p className="text-lg font-semibold">{s.value}</p><p className="text-xs text-ds-muted">{s.label}</p></div>)}</div></GlassCard>;}
